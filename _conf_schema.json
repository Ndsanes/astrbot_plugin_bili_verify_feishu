{
  "FEISHU_APP_ID": {
    "description": "飞书应用 App ID",
    "type": "string",
    "hint": "在飞书开放平台应用凭证页面获取，例如 cli_xxx",
    "default": "",
    "obvious_hint": true
  },
  "FEISHU_APP_SECRET": {
    "description": "飞书应用 App Secret",
    "type": "string",
    "hint": "在飞书开放平台应用凭证页面获取",
    "default": "",
    "obvious_hint": true
  },
  "FEISHU_APP_TOKEN": {
    "description": "飞书多维表格 App Token",
    "type": "string",
    "hint": "多维表格 URL 中的 app_token，示例: https://xxx.feishu.cn/base/APPTOKEN?table=TABLEID",
    "default": "",
    "obvious_hint": true
  },
  "FEISHU_TABLE_ID": {
    "description": "飞书多维表格 Table ID",
    "type": "string",
    "hint": "多维表格 URL 参数 table= 后面的值",
    "default": "",
    "obvious_hint": true
  },
  "FEISHU_STATUS_FIELD": {
    "description": "飞书状态字段名",
    "type": "string",
    "hint": "用于标记成员是否在群中，默认字段名为 状态",
    "default": "状态"
  },
  "FEISHU_STATUS_ACTIVE_VALUE": {
    "description": "成员在群中的状态值",
    "type": "string",
    "default": "在群"
  },
  "FEISHU_STATUS_LEFT_VALUE": {
    "description": "成员退群后的状态值",
    "type": "string",
    "default": "已退群"
  },
  "WHITELIST_GROUPS": {
    "description": "白名单群号列表",
    "type": "list",
    "hint": "仅白名单群启用登记流程。首次启动时会用于初始化持久化白名单。",
    "default": []
  },
  "MAX_RETRIES": {
    "description": "飞书写入失败最大重试次数",
    "type": "int",
    "default": 3
  },
  "RETRY_DELAY": {
    "description": "重试基础延迟（秒）",
    "type": "float",
    "default": 1.0,
    "hint": "采用指数退避: delay * 2^attempt,例如重试3次，基础延迟1秒，实际重试间隔分别为1s、2s、4s"
  },
  "ENABLE_PENDING_CHECK": {
    "description": "是否启用未处理入群请求巡检",
    "type": "bool",
    "default": true,
    "hint": "关闭后将不启动定时巡检任务，可用于临时停用巡检。（这个功能主要是防止机器人掉线后无法及时处理入群请求，重新上线后进行检查）"
  },
  "ENABLE_STARTUP_REQUEST_SCAN": {
    "description": "启动时是否补偿扫描历史加群请求",
    "type": "bool",
    "default": true,
    "hint": "开启后插件加载时会调用 get_group_system_msg 处理加载前未处理的加群请求。"
  },
  "STARTUP_REQUEST_SCAN_LIMIT": {
    "description": "启动补偿扫描最多处理请求数",
    "type": "int",
    "default": 50,
    "hint": "用于限制单次启动补偿扫描处理量，防止历史积压过大导致启动阻塞。"
  },
  "PENDING_CHECK_INTERVAL": {
    "description": "未处理入群请求巡检间隔（秒）",
    "type": "int",
    "default": 3800,
    "hint": "插件会按该间隔检查白名单群中的待补UID和写入失败记录，并输出告警日志。最小值为10秒。"
  },
  "ENABLE_OFFLINE_NOTIFY": {
    "description": "是否启用QQ机器人掉线检测与飞书通知",
    "type": "bool",
    "default": false,
    "hint": "开启后插件将定时检测 OneBot 连接状态，判定掉线后通过飞书API通知指定用户。需配置飞书通知目标。"
  },
  "OFFLINE_FEISHU_TARGETS": {
    "description": "飞书通知目标ID列表",
    "type": "list",
    "hint": "掉线时通过飞书私聊通知的用户ID列表。可填 open_id / user_id / union_id / email，类型由下方配置决定。需确保机器人有 im:message 权限且与目标有会话。",
    "default": []
  },
  "OFFLINE_FEISHU_ID_TYPE": {
    "description": "飞书目标ID类型",
    "type": "string",
    "hint": "对应 OFFLINE_FEISHU_TARGETS 的ID类型：open_id / user_id / union_id / email / chat_id",
    "default": "open_id"
  },
  "OFFLINE_CHECK_INTERVAL": {
    "description": "掉线检测间隔（秒）",
    "type": "int",
    "default": 60,
    "hint": "每隔该秒数调用 get_status 检查一次在线状态。最小值10秒，建议30-120秒。"
  },
  "OFFLINE_THRESHOLD": {
    "description": "连续失败阈值",
    "type": "int",
    "default": 3,
    "hint": "连续多少次检测失败才判定为掉线并通过飞书告警，避免网络抖动误报。最小值1。"
  },
  "OFFLINE_RECOVERY_NOTIFY": {
    "description": "恢复上线时是否通过飞书通知",
    "type": "bool",
    "default": true,
    "hint": "开启后机器人从掉线恢复时也会通过飞书通知。"
  },
  "REQUEST_DELAY_MIN_SECONDS": {
    "description": "收到入群请求后随机延迟最小值（秒）",
    "type": "float",
    "default": 10.0,
    "hint": "为防止被批量请求 gank，处理每个入群请求前会随机延迟 min~max 秒。设为 0 则不延迟。默认 10 秒。"
  },
  "REQUEST_DELAY_MAX_SECONDS": {
    "description": "收到入群请求后随机延迟最大值（秒）",
    "type": "float",
    "default": 60.0,
    "hint": "与最小值配合形成随机区间，建议 10~60 秒。若最大值小于最小值则按最小值处理。"
  }
}
